import SpeechView from "./SpeechView.js";

export default class GatheringForeignView extends SpeechView {
    constructor(onStateChange, view, formantProcessor) {
        console.log("GatheringForeignView constructor");
    }
}